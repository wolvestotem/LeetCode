#include<iostream>
#include<queue>
#include<algorithm>

using namespace std;

int main() {
	/*
	queue<int> ex;
	queue<int> myqueue;
	myqueue.push(3);
	myqueue.push(5);
	myqueue.push(7);
	cout << myqueue.size() <<'\t'<< myqueue.empty() << endl;
	myqueue.emplace(100);
	cout << myqueue.back() << endl;*/

	int n = 5;
	while (--n) {
		cout << "hello" << endl;
	}
}